# {Project Name}

{一句话说明项目用途。}

本文件在改动时遵循下面的规则：

- 常用命令、核心约束随代码演进持续修订，命令变了就改，约束不再成立就删。
- 文档索引只在新增或移除对应文档时改动。
- 编码风格、研发流程稳定后不需要每次改动都动，除非项目约定本身变动
- 发现条目已不成立或和实际代码矛盾时删除或改写，不追加新条目掩盖旧条目。
- 正文控制在 100 行以内。改动时先问“删掉这条 agent 会不会做错事”，答不上来就删；容量不够用精简换空间，而不是继续往下加。
## 文档索引

- `{ARCHITECTURE 文件名}`：{系统结构和模块边界。}
- `{WORKFLOW 文件名}`：{研发流程和推进范围。}
- `{ICAFE 文件名}`：{iCafe 使用约定。}
- {项目已有的其他高价值文档。}

## 常用命令

- 安装依赖：`{install command}`
- 启动开发环境：`{dev command}`
- 构建：`{build command}`
- 测试：`{test command}`
- 检查：`{lint or typecheck command}`

## 核心约束

- {容易被忽略的项目特有约束。}
- {生成代码、环境、测试或部署方面的注意事项。}

## 研发流程

{用 3 到 5 行说明分支模型、评审方式和提交前验证要求。}

## 编码风格（可选章节）

- {仅列出与语言或框架默认不同的约定。}